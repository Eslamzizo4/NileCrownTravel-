# Nile Crown Travel — خطوة النشر

## ما تم تجهيزه
- واجهة عربية وإنجليزية.
- صفحات الرحلات وتفاصيل الرحلة.
- واتساب: 01010575983.
- البريد: ziade642@gmail.com.
- لوحة إدارة أولية.
- `backend/server.js` كـ API حقيقي مبدئي باستخدام SQLite.
- `backend/package.json` لتشغيل الـ API.

## تشغيل الـ Backend محلياً
داخل مجلد backend:
1. `npm install`
2. `npm start`
3. الـ API يصبح على `http://localhost:3000`.

## قبل النشر العام
يلزم:
- استضافة للواجهة والـ API.
- قاعدة بيانات مستضافة أو نقل SQLite إلى PostgreSQL.
- تسجيل دخول حقيقي للوحة الإدارة.
- تخزين صور آمن.
- HTTPS.
- ربط الواجهة بالـ API بدلاً من LocalStorage.

لا توجد حسابات استضافة أو قاعدة بيانات خارجية متصلة في هذه النسخة، لذلك لا أدّعي أن الموقع منشور بالفعل.
